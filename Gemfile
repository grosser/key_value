source :rubygems

group :dev do # not development <-> would add unneeded development dependencies in gemspec
  gem 'activerecord', ENV['RAILS']
  gem 'sqlite3', '~>1.3.4'
  #gem 'mysql2', (ENV['RAILS'].include?('2') ? '~>0.2.0' : '~>0.3.0')
  gem 'handlersocket'
  gem 'rake'
  gem 'rspec', '~>2'
  gem 'jeweler'
end
